<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="icon" type="image/png" href="img/icon_roller.png">
    <title>Roller-Blade</title>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light">
            <div class="container-fluid" style="background-color: #c7acd1;">
              <a class="navbar-brand" href="{{ path('default_index') }}">
                <img src="{{'/img/logo_roller3.png'}}" style="width:90%; height:90%" alt="">
              </a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                    <a class="nav-link" href="#">Produit</a>
                  </li>
                   <li class="nav-item">
                    <a class="nav-link" href="#">Evenement</a>
                  </li>
                </ul>
              </div>
               <div class="navbar-nav ms-auto">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <div style="margin-top:9px;color:#FFF;font-size:20px;display:flex;margin-right:30px;">
                        Bonjour {{ app.user.name }} {{ app.user.firstname }}
                    <a class="nav-link profil" style="font-size:25px;color:#FFF;margin-top:-8px;margin-left:10px;" href="{{ path('user_profil') }}">
                        <i class="fas fa-user"></i>
                    </a>
                    </div>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="nav-link button ajouter lien-responsive"" href="">Gestion</a>
                    {% endif %}
                    <a class="nav-link button deconnexion lien-responsive"" href="{{ path('app_logout') }}">Déconnexion</a>
                    {% else %}
                     <div class="collapse navbar-collapse" id="navbarSupportedContent" style="display:none";>
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                          <li class="nav-item">
                            <a class="nav-link" href="#">Inscription</a>
                          </li>
                          <li class="nav-item">
                            <a class="nav-link" href="#">Connexion</a>
                          </li>
                        </ul>
                    </div>
                      {% endif %}
                </div>
             
            </div>
          </nav>
    </header>
</body>
</html>